# Secret Sender Bot (Telegram) 🤖

Этот бот позволяет пользователям анонимно отправлять контент (текст, фото, видео) администраторам. Администраторы могут проверять предложенные посты и публиковать их в канал или отклонять нажатием одной кнопки.

## ✨ Возможности

* **Анонимность:** Пользователи отправляют контент боту, а админы видят только сам контент (без указания автора).
* **Модерация:** Удобные кнопки «✅ Опубликовать» и «❌ Отклонить».
* **Поддержка медиа:** Работает с текстом и медиафайлами (благодаря методу `copy_to`).
* **Мульти-админка:** Уведомления приходят всем администраторам, указанным в настройках.
* **Синхронизация:** Если один админ принял/отклонил пост, кнопки исчезают у всех остальных.
* **База данных:** Используется SQLite для хранения статусов тикетов.

## 🛠 Установка и запуск

### Предварительные требования
* Python 3.8 или выше
* Токен бота от [@BotFather](https://t.me/BotFather)

### Шаг 1. Клонирование репозитория
Скачайте проект в удобную папку.

### Шаг 2. Создание виртуального окружения (рекомендуется)
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux / macOS
python3 -m venv venv
source venv/bin/activate
```
Шаг 3. Установка зависимостей
Установите необходимые библиотеки (aiogram, python-dotenv):
```bash
pip install aiogram python-dotenv
```
Шаг 4. Настройка конфигурации
Переименуйте файл example.env в .env.
Откройте .env и заполните ваши данные:
```Ini, TOML
BOT_TOKEN=123456:Ваш-Токен-От-BotFather
CHANNEL_ID=-1001234567890  # ID канала, куда будут публиковаться посты
ADMIN_IDS=123456789,987654321 # ID админов через запятую
```

Как узнать ID?
ADMIN_IDS: Напишите боту /start (если вы уже вписали примерный ID) или используйте стороннего бота (например, @userinfobot).
CHANNEL_ID: Перешлите сообщение из канала в бот @getmyid_bot или добавьте этого бота в канал. ID канала обычно начинается с -100.

Шаг 5. Запуск бота
```bash
python bot.py
```

🚀 Использование
Пользователь отправляет сообщение боту (картинку, текст и т.д.).
Бот пересылает сообщение всем Администраторам.
Администратор видит сообщение с кнопками:

Опубликовать: Пост мгновенно отправляется в указанный канал. Пользователю (если бы была обратная связь) ничего не отправляется, но статус в базе меняется.

Отклонить: Пост помечается как отклоненный и никуда не публикуется.

После нажатия кнопки, клавиатура удаляется у всех админов, чтобы избежать дублирования действий.

📂 Структура проекта
bot.py — Основной код бота.

bot_database.db — База данных (создается автоматически при первом запуске).

.env — Файл с конфиденциальными настройками (не выкладывайте его в публичный доступ!).

.gitignore — Список файлов, игнорируемых Git.

📄 Лицензия
Этот проект распространяется под лицензией MIT. Подробности см. в файле LICENSE.
